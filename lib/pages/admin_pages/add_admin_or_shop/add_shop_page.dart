import 'package:flutter/material.dart';

class AddShopPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print('Add Shop Page is Build');
    return Center(
      child: Text('Add Shop Page'),
    );
  }
}
