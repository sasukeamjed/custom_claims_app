import 'package:flutter/material.dart';

class StatsPage extends StatelessWidget{
  //ToDo: check if the user got admin claim or not
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Stats Page'),
    );
  }
}